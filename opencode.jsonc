{
  // Global orchestration config — merge the relevant sections into your ~/.config/opencode/opencode.jsonc.
  //
  // No config entries are required for this orchestrator by default:
  // - Custom agents are auto-discovered from ~/.config/opencode/{agent,agents}/.
  // - shared_context.md is loaded explicitly by the orchestrator, not via "instructions".
  // - Tool permissions are enforced by agent prompts, not config.
}
